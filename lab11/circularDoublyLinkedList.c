#include <stdio.h>

struct node
{
  struct node *prev;
  int info;
  struct node *next;

  struct
} typedef struct node node;

void insert_from_beginning(node **prev, node **next)
{
  
}

int main()
{

  return 0;
}